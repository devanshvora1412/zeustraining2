function validateForm(uname,ucomments) {
    var uname = document.forms["myForm"]["name"].value
    var ucomments = document.forms["myForm"]["comments"].value
    if (uname == "" || ucomments == "") {
      alert("Name must be filled out");
      return false;
    }
    if 
  }